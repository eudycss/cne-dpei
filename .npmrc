# Metro (Expo/React Native) no soporta bien la estructura de node_modules
# anidada de pnpm. Aplanamos el árbol para que Metro resuelva las
# dependencias (directas y transitivas) de Expo. Recomendado para
# monorepos pnpm + React Native.
node-linker=hoisted
shamefully-hoist=true
